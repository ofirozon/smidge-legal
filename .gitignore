# Internal notes. This repo is PUBLIC; the README explains why the app repo is
# private, names internal paths, and is written for us rather than for a reader
# of the policy. It stays on disk and out of the publish.
README.md
